{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "astro",
    "buildCommand": "cd ../.. && pnpm --filter @ever-works/web-minimal build",
    "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
    "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"master\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"develop\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"stage\" ]; then exit 1; else exit 0; fi",
    "github": {
        "autoJobCancelation": true
    }
}
