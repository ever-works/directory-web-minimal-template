/**
 * Mount fixture entry for Playwright Component Testing.
 *
 * This file runs in the browser before each component test mount. It is
 * the place to import global styles, register Web Components, or
 * initialize any browser-side scaffolding the components-under-test
 * depend on.
 *
 * Currently empty — `@ever-works/ui` headless components do not require
 * any global setup. Add imports here as the CT corpus grows (e.g.
 * `import './test-globals.css'` if a future test needs base styling).
 */
export {};
