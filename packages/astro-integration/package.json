{
    "name": "@ever-works/astro-integration",
    "version": "0.1.0",
    "description": "Astro integration that bridges the Ever Works plugin system into Astro's build lifecycle",
    "type": "module",
    "exports": {
        ".": "./src/index.ts",
        "./webhook-endpoint": "./src/webhook-endpoint.ts"
    },
    "files": ["src"],
    "scripts": {
        "typecheck": "tsc --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "@ever-works/plugins": "workspace:*",
        "@ever-works/core": "workspace:*",
        "@ever-works/sync": "workspace:*"
    },
    "devDependencies": {
        "astro": "^6.0.0",
        "typescript": "^5.8.3",
        "vitest": "^4.1.4"
    },
    "peerDependencies": {
        "astro": "^6.0.0"
    }
}
